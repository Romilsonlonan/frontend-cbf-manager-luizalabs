export interface PendingApprovalCardProps {
    children: React.ReactNode;
}

export interface PendingApprovalHeaderProps {
    title: string;
    description: string;
}

export interface PendingApprovalIconProps {
    icon: React.ElementType;
}

export interface PendingApprovalActionsProps {
    children: React.ReactNode;
}

export interface PendingApprovalContentProps {
    children: React.ReactNode;
}
